# Project Agent Rules

## Automated Graph Update Rule
- After any code refactoring, edit, or file modification in this codebase, automatically run `graphify . --code-only` (or `graphify .`) in the terminal to keep the architecture map in `graphify-out/` updated.
